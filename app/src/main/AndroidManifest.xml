<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.zxcvbn.bookstore">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="provider.GlobalData"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/MyMaterialTheme">
        <activity
            android:name="activity.SplashScreen"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="activity.MainActivity"
            android:label="@string/app_name" />
        <activity
            android:name="activity.LoginFormActivity"
            android:label="@string/app_name" />
        <activity
            android:name="activity.RegisterActivity"
            android:label="@string/app_name" />
        <activity
            android:name="activity.AnimateToolbar"
            android:label="@string/app_name" />
        <activity android:name="activity.Detail_Book" />
        <activity
            android:name="activity.DetailAuthor"
            android:label="@string/app_name" />
        <activity android:name="activity.activity_start" />
        <activity android:name="activity.activity_end" />
        <activity android:name="activity.shopping_cart"></activity>
    </application>

</manifest>